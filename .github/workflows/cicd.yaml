name: CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  cicd:
    name: CI/CD
    uses: dmikalova/github-meta/.github/workflows/go-cloudrun.yaml@main
    permissions:
      contents: write
      id-token: write
      packages: write
    secrets: inherit
